TODO: Flesh this out. This is a very much a work in progress still

Zodiac uses the Python Nose testing framework. To run all tests, execute nosetests from the top
level directory. 

TODO: Decide on tagging methodology
To run proper subsets of tests, use the -a argument with the desired tag.

Next items
-Backfill comments
-Decide on tagging name/types
-Test using Nose's multithreaded execution
